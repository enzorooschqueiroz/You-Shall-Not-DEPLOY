name: Deploy

on:
  push:
    branches: [main]

  workflow_dispatch:

jobs:
  check_deploy_date:
    runs-on: ubuntu-latest
    steps:
      - name: code checkout
        uses: actions/checkout@v3

      - name: set-up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.9'

      - name: Verify deploy date
        run: |
          python check_deploy_date.py

   deploy:
    # Este job depende da verificação de data ser bem-sucedida
    needs: check_deploy_date
    runs-on: ubuntu-latest
    steps:
      - name: Code checkout
        uses: actions/checkout@v3

      # Etapas personalizáveis para deploy
      # Substitua pelos seus próprios comandos de deploy
      - name: Configure seu deploy aqui
        run: |

  
